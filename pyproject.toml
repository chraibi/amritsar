[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["amritsar.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "W", "B", "UP", "SIM"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
